Notes to self,

run below on cluster to get model. dataset loads on its own. 
python -c "from huggingface_hub import snapshot_download; snapshot_download(repo_id='Qwen/Qwen3-0.6B',  local_dir='/models/Qwen3-0.6B', local_dir_use_symlinks=False)"

